# ── 本地工作目录（豁免，不上传）─────────────────────
.workbuddy/

# ── 生成脚本（豁免，不上传）─────────────────────────
build_ppt.py

# ── Office 临时锁文件 ───────────────────────────────
~$*
*.tmp

# ── 过程性校验产物（豁免）───────────────────────────
_render/
_verify/
_crop/
